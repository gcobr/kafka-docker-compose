info:
  name: Empty data from a topic
  type: http
  seq: 6
  tags:
    - topic_data

http:
  method: DELETE
  url: "{{baseUrl}}/api/:cluster/topic/:topicName/data/empty"
  params:
    - name: cluster
      value: ""
      type: path
    - name: topicName
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: emptyTopic 200 response
    request:
      url: "{{baseUrl}}/api/:cluster/topic/:topicName/data/empty"
      method: DELETE
      params:
        - name: cluster
          value: ""
          type: path
        - name: topicName
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
