info:
  name: Delete a schema
  type: http
  seq: 7
  tags:
    - schema_registry

http:
  method: DELETE
  url: "{{baseUrl}}/api/:cluster/schema/:subject"
  params:
    - name: cluster
      value: ""
      type: path
    - name: subject
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: delete_2 200 response
    request:
      url: "{{baseUrl}}/api/:cluster/schema/:subject"
      method: DELETE
      params:
        - name: cluster
          value: ""
          type: path
        - name: subject
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
