info:
  name: List all schemas
  type: http
  seq: 10
  tags:
    - schema_registry

http:
  method: GET
  url: "{{baseUrl}}/api/:cluster/schemas"
  params:
    - name: cluster
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: listAll 200 response
    request:
      url: "{{baseUrl}}/api/:cluster/schemas"
      method: GET
      params:
        - name: cluster
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            ""
          ]
