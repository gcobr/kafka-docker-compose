info:
  name: Partition counts
  type: http
  seq: 2
  tags:
    - topic

http:
  method: GET
  url: "{{baseUrl}}/api/:cluster/node/partitions"
  params:
    - name: cluster
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: nodePartitions 200 response
    request:
      url: "{{baseUrl}}/api/:cluster/node/partitions"
      method: GET
      params:
        - name: cluster
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "id": 0,
              "countLeader": 0,
              "countInSyncReplicas": 0,
              "totalPartitions": 0
            }
          ]
