info:
  name: Increase partition for a topic
  type: http
  seq: 15
  tags:
    - topic

http:
  method: POST
  url: "{{baseUrl}}/api/:cluster/topic/:topicName/partitions"
  params:
    - name: cluster
      value: ""
      type: path
    - name: topicName
      value: ""
      type: path
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: increasePartition 200 response
    request:
      url: "{{baseUrl}}/api/:cluster/topic/:topicName/partitions"
      method: POST
      params:
        - name: cluster
          value: ""
          type: path
        - name: topicName
          value: ""
          type: path
      body:
        type: json
        data: "{}"
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
