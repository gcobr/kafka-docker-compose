info:
  name: Retrieve the last record for a list of topics
  type: http
  seq: 5
  tags:
    - topic

http:
  method: GET
  url: "{{baseUrl}}/api/:cluster/topic/last-record"
  params:
    - name: cluster
      value: ""
      type: path
    - name: topics
      value: "[]"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: lastRecord 200 response
    request:
      url: "{{baseUrl}}/api/:cluster/topic/last-record"
      method: GET
      params:
        - name: cluster
          value: ""
          type: path
        - name: topics
          value: "[]"
          type: query
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
