info:
  name: List all nodes
  type: http
  seq: 1
  tags:
    - node

http:
  method: GET
  url: "{{baseUrl}}/api/:cluster/node"
  params:
    - name: cluster
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: list_4 200 response
    request:
      url: "{{baseUrl}}/api/:cluster/node"
      method: GET
      params:
        - name: cluster
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "id": "",
            "nodes": [
              {
                "id": 0,
                "host": "",
                "port": 0,
                "rack": ""
              }
            ],
            "controller": {}
          }
