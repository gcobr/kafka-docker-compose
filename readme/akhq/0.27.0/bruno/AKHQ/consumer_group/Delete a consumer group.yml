info:
  name: Delete a consumer group
  type: http
  seq: 4
  tags:
    - consumer_group

http:
  method: DELETE
  url: "{{baseUrl}}/api/:cluster/group/:groupName"
  params:
    - name: cluster
      value: ""
      type: path
    - name: groupName
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: delete_1 200 response
    request:
      url: "{{baseUrl}}/api/:cluster/group/:groupName"
      method: DELETE
      params:
        - name: cluster
          value: ""
          type: path
        - name: groupName
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
