info:
  name: Retrieve consumer group offsets
  type: http
  seq: 7
  tags:
    - consumer_group

http:
  method: GET
  url: "{{baseUrl}}/api/:cluster/group/:groupName/offsets"
  params:
    - name: cluster
      value: ""
      type: path
    - name: groupName
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: offsets 200 response
    request:
      url: "{{baseUrl}}/api/:cluster/group/:groupName/offsets"
      method: GET
      params:
        - name: cluster
          value: ""
          type: path
        - name: groupName
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {}
          ]
